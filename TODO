add a dialog to select/filter folder of pictures
add a dialog to change:
	- grid size
	- card size (default 100 x 100)
	- Players numbers/names
add a dialog to view Highscores (name, victories)
log all games: game-id { id -> width x height} , players {names -> card pairs}
add sounds from SDLx::Sound (require SDL + Alien::SDL)
look at packer methods and try resizer handlers or let board-window not resizeable.
make text i18n
look for free redistribuable picts.
document how images must be installed ( ./images for card's back face and ./images/XXX/* )